// Runtime settings. In a deployment this file is generated at container start (see the
// Dockerfile), so one built image works in every environment. Leave empty in development:
// the defaults in src/config.ts point at the local Keycloak and the Vite dev proxy.
window.__APP_CONFIG__ = {};
